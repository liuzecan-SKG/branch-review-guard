---
description: Performance review checklist for a single backend endpoint/Service (stack-agnostic). Consult when reviewing or optimizing an interface after a requirement is completed.
globs:
alwaysApply: false
---

# 接口性能 Review 提醒（栈无关）

需求开发完成后、或被要求 review / 优化某个接口性能时，按下面口诀做基于证据的复盘；完整流程、栈特有规则加载与报告模板见 skill `endpoint-perf-review`。

口诀：先量化 → 砍多余 → 批量化 → 缓存 → 并发降级 → 特殊路径 → 测正确 → 留观测。

- 先量化：先数清单次请求的 远程(RPC)/DB/缓存 调用数，区分缓存命中 vs 冷启动最坏情况；无基线不优化。
- 砍多余：窄需求别复用重量级聚合；去重复调用；只取返回真正用到的实体与字段。
- 批量化：N+1、"全量找单条" → 改批量或定向单查；优先轻量投影方法。
- 缓存：复用已有缓存；静态配置进程内缓存；热点共享键做单飞/防击穿（必要时分布式锁串行回源）。
- 并发降级：独立 I/O 并行用专用有界池（勿复用 scheduled / 公共池跑阻塞调用）；请求上下文（用户/租户/语言/时区）不跨线程，提交任务前显式捕获传递；非关键调用失败降级；收紧 RPC/HTTP 超时并熔断。
- 特殊路径：虚拟/演示/合成数据不要走真实回源（会写空壳）；预置或幂等回填；读写用同一套 key。
- 测正确：优化窄路径不要改共享代码行为——抽共享内核 + 补测试；保鉴权与幂等。
- 留观测：加分阶段耗时/指标；热点单条查询 `EXPLAIN` 确认命中索引。

注意：这是提问清单不是必做项，按基线 ROI 取舍，避免过度缓存/过度并行。

> 同栈团队可在套件 `rules/` 启用对应规则包，获得机制级深度检查；未启用则只跑上面的通用提醒。
